-- repeat.vim for repeating supported plugin maps with "."
-- https://github.com/tpope/vim-repeat

return {
  'tpope/vim-repeat',
  -- Load the plugin whenever Neovim starts
  -- You could use event = 'VeryLazy' instead if you want to lazy-load it
}
